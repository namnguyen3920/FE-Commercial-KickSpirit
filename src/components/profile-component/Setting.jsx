import React from "react";

const Setting = () => {
  return (
    <div className="p-6">
      <h2 className="text-2xl font-bold mb-4">Setting</h2>
      <p>View items and lists you've saved here.</p>
    </div>
  );
};

export default React.memo(Setting);
